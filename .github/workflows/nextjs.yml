name: MFP client 빌드

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      with:
        ref: develop  # develop 브랜치를 체크아웃

    - name: Set up SSH
      uses: webfactory/ssh-agent@v0.5.1
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: SSH and Run Script
      run: |
        ssh -o "StrictHostKeyChecking=no" -p 31220 billi@129.154.207.190 << 'EOF'
          cd /home/billi/Projects/mad-for-post-client
          git pull
          pm2 delete "mad4post-client"
          npm run build
          pm2 start npm --name "mad4post-client" -- run start 
          echo "Script execution completed"
        EOF
